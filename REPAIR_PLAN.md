# MatchShell 渲染修复计划（2026-09-05）

## 现象与证据

- OPPO PKT110 上首页可打开，但卡顿、比例轻微挤压；点击“资源”后页面白屏，仍有滚动条。
- `https://hcgy2018.site/resources/` 当前返回 HTTP 200 和完整 HTML；首页图片预览响应为 505,733 bytes，不能归因于资源接口空内容或 53.1 MB 原文件。
- 壳配置同时启用了 `useWideViewPort` 和 `loadWithOverviewMode`，后者会强制初始缩放；资源页又在 900px 处切换到带固定视口/隐藏溢出的桌面布局。
- Manifest 显式关闭了整个应用的硬件加速。这不适合全屏 WebView，可能导致复杂 CSS 绘制和滚动性能异常。

## 第一版范围

1. 恢复应用硬件加速。
2. 关闭 WebView 概览缩放，保留 viewport meta 支持。
3. 不修改服务端、资源页 HTML/CSS、URL 存储或网络安全策略。

## 图标微调（2026-09-05）

- 启动器前景徽标以 12dp 等距内边距缩小并保持居中；普通、圆形和单色自适应图标复用同一前景，避免启动器形状或主题变化时尺寸不一致。
- 不改动原始 PNG、应用内页面或应用名称。

## 验证 Gate

- G1：在纯 ASCII `TEMP`/`TMP` 下完成 `assembleDebug` 与 `lintDebug`。
- G2：确认新 APK 的 SHA-256。
- G2a：资源编译确认自适应图标的普通、圆形与单色层均可解析。
- G3（未自动执行）：在 OPPO PKT110 安装后，记录首页流畅度、资源页可见内容、CSS `innerWidth` 与控制台错误。G1/G2 不能替代 G3。

## 风险与回退

- 若 G3 显示问题仅在硬件加速下复现，不能重新全局关闭硬件加速；应以设备截图、WebView 控制台和页面尺寸为依据，定位单一页面或单一绘制特性。
- 工程没有 Git 仓库，改动依据本计划及逐文件 diff 审查，不具备正常分支回退能力。
